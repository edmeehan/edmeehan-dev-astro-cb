---
import CustomSection from "@builders/custom-section/CustomSection.astro";
import Heading from "@core-elements/heading/Heading.astro";
import Image from "@core-elements/image/Image.astro";
import SimpleText from "@core-elements/simple-text/SimpleText.astro";
import Text from "@core-elements/text/Text.astro";
import ButtonGroup from "@wrappers/button-group/ButtonGroup.astro";
import Grid from "@wrappers/grid/Grid.astro";
import GridItem from "@wrappers/grid/GridItem.astro";

const {
  eyebrow = "",
  heading = "",
  subtext = "",
  buttonSections = [],
  images,
  backgroundColor,
  class: className,
  _component,
  ...htmlAttributes
} = Astro.props;
---

<CustomSection
  class:list={["hero-story", className]}
  maxContentWidth="2xl"
  paddingHorizontal="lg"
  paddingVertical="none"
  colorScheme="inherit"
  backgroundColor={backgroundColor}
  {...htmlAttributes}
>
  <Grid layout="start" class:list={["hero-story__grid"]}>
    <GridItem class:list={["hero-story__text"]}>
      <SimpleText
        class="eyebrow"
        data-prop="eyebrow"
        size="sm"
        text={eyebrow}
      />
      <Heading
        level="h1"
        size="xl"
        data-prop="heading"
      >
        {heading}
      </Heading>
      <Text
        class="subtext"
        data-prop="subtext"
        text={subtext}
      />
      <ButtonGroup
        class="buttonSections"
        buttonSections={buttonSections}
        data-children-prop="buttonSections"
      />
    </GridItem>
    <GridItem class:list={["hero-story__img-primary"]}>
      <Image
        source={images.imageSource_1}
        alt={images.imageAlt_1}
        rounded={true}
        aspectRatio="portrait"
        data-prop-src="images.imageSource_1"
        data-prop-alt="images.imageAlt_1"
        priority={true}
      />
    </GridItem>
    <GridItem class:list={["hero-story__img-secondary"]}>
      <Image
        source={images.imageSource_2}
        alt={images.imageAlt_2}
        rounded={true}
        aspectRatio="portrait"
        data-prop-src="images.imageSource_2"
        data-prop-alt="images.imageAlt_2"
        priority={true}
      />
    </GridItem>
    <GridItem class:list={["hero-story__img-tertiary"]}>
      <Image
        source={images.imageSource_3}
        alt={images.imageAlt_3}
        rounded={true}
        aspectRatio="horizontal-strip"
        data-prop-src="images.imageSource_3"
        data-prop-alt="images.imageAlt_3"
        priority={true}
      />
    </GridItem>
  </Grid>
</CustomSection>

<style>
  @import "/src/styles/variables/_breakpoints.css";

  @layer page-section {
    .hero-story {
      :global(.heading) + .subtext {
        margin-top: 0;
      }
      .subtext + .buttonSections {
        margin-top: var(--spacing-2xl);
      }
    }

    @media (--bp-md-down) {
      .hero-story__grid {
        :global(> .grid-inner.layout-start) {
          grid-template-columns: 1fr;
        }
      }
    }

    @media (--bp-md) {
      .hero-story__grid {
        :global(> .grid-inner.layout-start) {
          grid-template-columns: repeat(12, 1fr);
          grid-template-rows: minmax(20vh, 300px) auto 150px;
          gap: 0;
        }
      }
      .hero-story__text {
        grid-column: 6 / span 7;
        grid-row: 2;
      }
      .hero-story__img-primary {
        grid-column: 1 / span 4;
        grid-row: 1 / span 3;
        align-self: center;

        :global(> .grid-item-content) {
          height: auto;
        }
      }
      .hero-story__img-secondary {
        grid-column: 9 / span 3;
        grid-row: 1 / span 2;
        position: absolute;

        :global(.image) {
          position: relative;
          top: -100px;
        }
      }
      .hero-story__img-tertiary {
        grid-column: 9 / span 4;
        grid-row: 3;
      }
    }

    @media (--bp-xl) {
      .hero-story__text {
        grid-column: 6 / span 5;
      }
      .hero-story__img-primary {
        grid-column: 1 / span 4;
      }
      .hero-story__img-secondary {
        grid-column: 9 / span 3;
      }
      .hero-story__img-tertiary {
        grid-column: 9 / span 4;
      }
    }
  }
</style>
